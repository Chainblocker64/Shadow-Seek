services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: shadow_seek
    ports:
      - '5432:5432'
    volumes:
      - shadowseek_db:/var/lib/postgresql

volumes:
  shadowseek_db:
