name: Auto-Deploy
on:
  push:
    branches: [main]
    paths:
      - "backend/**"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger backend deploy
        run: curl -fsSL -X POST "${{ secrets.DEPLOY_HOOK_URL }}"
